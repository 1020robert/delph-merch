PORT=3000
OWNER_EMAIL=1020rjl@gmail.com
SESSION_SECRET=replace-with-a-long-random-string
DATA_DIR=./data
COOKIE_SECURE=false

# Optional: only used if password gate is re-enabled in code.
LOGIN_PASSWORD=replace-with-shared-club-password

# Optional: owner notification emails for new orders.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
SMTP_SECURE=false
